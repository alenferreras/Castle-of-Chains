extends Node2D

@onready var label = $Debug/Label
@onready var label2 = $Debug/Label2

#func _physics_process(_delta):
	#label.text = "velocity_y = " + str(player_character.player.velocity.y)#"ball velocity = " + str(0.075 * ball.linear_velocity.length())
	#label2.text = "launch angle = " + str(player_character.launch_angle)
